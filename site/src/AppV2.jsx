import React, { useEffect, useMemo, useState } from 'react'
import { Area, AreaChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import './accountability.css'

const BASE = import.meta.env.BASE_URL
const TEAM_NAMES = {
  ARI:'Arizona Cardinals',ATL:'Atlanta Falcons',BAL:'Baltimore Ravens',BUF:'Buffalo Bills',CAR:'Carolina Panthers',CHI:'Chicago Bears',CIN:'Cincinnati Bengals',CLE:'Cleveland Browns',DAL:'Dallas Cowboys',DEN:'Denver Broncos',DET:'Detroit Lions',GB:'Green Bay Packers',HOU:'Houston Texans',IND:'Indianapolis Colts',JAC:'Jacksonville Jaguars',JAX:'Jacksonville Jaguars',KC:'Kansas City Chiefs',LAC:'Los Angeles Chargers',LA:'Los Angeles Rams',LV:'Las Vegas Raiders',MIA:'Miami Dolphins',MIN:'Minnesota Vikings',NE:'New England Patriots',NO:'New Orleans Saints',NYG:'New York Giants',NYJ:'New York Jets',PHI:'Philadelphia Eagles',PIT:'Pittsburgh Steelers',SEA:'Seattle Seahawks',SF:'San Francisco 49ers',TB:'Tampa Bay Buccaneers',TEN:'Tennessee Titans',WAS:'Washington Commanders'
}

function parseCSV(text){const rows=[];let row=[],cell='',quoted=false;for(let i=0;i<text.length;i++){const c=text[i];if(quoted){if(c==='"'&&text[i+1]==='"'){cell+='"';i++}else if(c==='"')quoted=false;else cell+=c}else if(c==='"')quoted=true;else if(c===','){row.push(cell);cell=''}else if(c==='\n'){row.push(cell);rows.push(row);row=[];cell=''}else if(c!=='\r')cell+=c}if(cell.length||row.length){row.push(cell);rows.push(row)}const h=rows.shift()||[];return rows.filter(r=>r.some(Boolean)).map(r=>Object.fromEntries(h.map((x,i)=>[x,r[i]??''])))}
async function fetchCSV(path){try{const r=await fetch(`${BASE}data/${path}`,{cache:'no-store'});if(!r.ok)return[];return parseCSV(await r.text())}catch{return[]}}
async function fetchJSON(path,fallback={}){try{const r=await fetch(`${BASE}data/${path}`,{cache:'no-store'});if(!r.ok)return fallback;return await r.json()}catch{return fallback}}
const n=v=>{if(v===''||v==null)return null;const x=Number(v);return Number.isFinite(x)?x:null}
const pct=(v,d=1)=>n(v)==null?'—':`${(n(v)*100).toFixed(d)}%`
const one=v=>n(v)==null?'—':n(v).toFixed(1)
const signed=v=>n(v)==null?'—':`${n(v)>0?'+':''}${n(v).toFixed(1)}`
const pp=v=>n(v)==null?'—':`${n(v)>0?'+':''}${n(v).toFixed(1)} pp`
const teamName=t=>TEAM_NAMES[t]||t||'—'
function american(p){p=n(p);if(p==null||p<=0||p>=1)return'—';const x=p>=.5?-100*p/(1-p):100*(1-p)/p;return`${x>0?'+':''}${Math.round(x)}`}
function kickoff(g){if(!g?.gameday)return null;const[hh='00',mm='00']=(g.gametime||'').split(':');const d=new Date(`${g.gameday}T${hh}:${mm}:00-04:00`);return Number.isNaN(d.getTime())?null:d}
function kickoffText(g){const d=kickoff(g);return d?new Intl.DateTimeFormat('en-US',{weekday:'short',month:'short',day:'numeric',hour:'numeric',minute:'2-digit',timeZone:'America/Los_Angeles'}).format(d):g?.gameday||'TBD'}
function line(m,h,a){m=n(m);if(m==null)return'—';if(Math.abs(m)<.05)return'PK';return m>0?`${h} -${Math.abs(m).toFixed(1)}`:`${a} -${Math.abs(m).toFixed(1)}`}
function normalizeGame(g){const hp=n(g.final_home_prob),pick=g.pick||(hp>=.5?g.home_team:g.away_team),pickP=hp==null?null:pick===g.home_team?hp:1-hp,m=n(g.expected_margin),market=n(g.spread_line),edge=m==null||market==null?null:(pick===g.home_team?m-market:market-m);return{...g,homeP:hp,pick,pickP,margin:m,market,pickEdge:edge}}
function strengthClass(e){const s=(e?.strength||'').toLowerCase();return['strong','moderate','weak'].includes(s)?s:'context'}

function Header({tab,setTab,status}){const tabs=[['week','Week'],['teams','Teams'],['ratings','Power Ratings'],['models','Model Lab'],['history','History'],['method','Methodology']];return <><header className="topbar"><div className="brand"><div className="brand-mark">NF</div><div><div className="brand-title">NFL FORECAST</div><div className="brand-sub">Sujar+ quantitative game intelligence</div></div></div><div className={`health ${status?.status==='healthy'?'healthy':''}`}><span/>{status?.status==='healthy'?'MODEL LIVE':'MODEL STATUS'}</div></header><nav className="nav">{tabs.map(([k,l])=><button key={k} className={tab===k?'active':''} onClick={()=>setTab(k)}>{l}</button>)}</nav></>}

function ProbabilityBar({game}){const hp=game.homeP??.5,ap=1-hp;return <div className="prob-wrap"><div className="prob-labels"><span>{game.away_team} {pct(ap,0)}</span><span>{pct(hp,0)} {game.home_team}</span></div><div className="probbar"><div className="away-fill" style={{width:`${ap*100}%`}}/><div className="home-fill" style={{width:`${hp*100}%`}}/></div></div>}
function ConfidenceRing({row}){const score=n(row?.confidence_index);return <div className="confidence-ring" style={{'--score':`${score??0}`}}><b>{score==null?'—':Math.round(score)}</b><span>CONF</span></div>}
function SourceFreshness({sources}){const rows=Object.entries(sources||{}).filter(([,v])=>v&&typeof v==='object'&&v.status);return <div className="freshness"><span>SOURCE HEALTH</span>{rows.slice(0,8).map(([k,v])=><div key={k}><b>{k.replaceAll('_',' ')}</b><em className={v.status==='healthy'?'ok':'warn'}>{v.status}</em></div>)}</div>}

function Hero({games,status,brief}){const next=[...games].filter(g=>kickoff(g)&&kickoff(g)>new Date()).sort((a,b)=>kickoff(a)-kickoff(b))[0];const strong=brief?.strongest?.[0]||[...games].sort((a,b)=>(b.pickP||0)-(a.pickP||0))[0];const edge=[...games].filter(g=>g.pickEdge!=null).sort((a,b)=>b.pickEdge-a.pickEdge)[0];return <section className="hero"><div className="hero-copy"><div className="eyebrow">2026 • WEEK {games[0]?.week||brief?.week||'—'}</div><h1>Every game.<br/><span>One accountable forecast.</span></h1><p>Live probabilities, scheme and personnel intelligence, forecast-movement attribution, calibration, and immutable pregame locks.</p></div><div className="hero-stats"><div><span>NEXT GAME</span><b>{next?`${next.away_team} @ ${next.home_team}`:'Slate complete'}</b><small>{next?kickoffText(next):''}</small></div><div><span>STRONGEST PICK</span><b>{strong?.pick?`${strong.pick} ${pct(strong.pick_prob??strong.pickP)}`:'—'}</b><small>{strong?.confidence||''}</small></div><div><span>BIGGEST EDGE</span><b>{edge?`${edge.pick} ${signed(edge.pickEdge)} pts`:'—'}</b><small>model vs market</small></div><div><span>LAST MODEL RUN</span><b>{status?.generated_utc?new Date(status.generated_utc).toLocaleTimeString('en-US',{hour:'numeric',minute:'2-digit',timeZone:'America/Los_Angeles'}):'—'}</b><small>{status?.data_state||''}</small></div></div></section>}

function WeeklyBrief({brief,games,movement}){const strongest=brief?.strongest?.length?brief.strongest:[...games].sort((a,b)=>(b.pickP||0)-(a.pickP||0)).slice(0,3);const uncertain=brief?.most_uncertain?.length?brief.most_uncertain:[...games].sort((a,b)=>Math.abs((a.pickP||.5)-.5)-Math.abs((b.pickP||.5)-.5)).slice(0,3);const movers=brief?.biggest_movers?.length?brief.biggest_movers:[...movement].sort((a,b)=>Math.abs(n(b.pick_delta_pp)||0)-Math.abs(n(a.pick_delta_pp)||0)).slice(0,3);return <section className="outlook"><div className="section-head"><div><span>WEEKLY OUTLOOK</span><h2>What changed — and why</h2></div><p>The editorial layer summarizes quantified signals without rewriting the forecast.</p></div><div className="outlook-grid"><div><span>Highest conviction</span>{strongest.map((g,i)=><b key={g.game_id||i}>{i+1}. {g.pick} <em>{pct(g.pick_prob??g.pickP)}</em></b>)}</div><div><span>Biggest movers</span>{movers.length?movers.map((g,i)=><b key={g.game_id||i}>{g.pick||g.game_id} <em>{pp(g.pick_delta_pp)} · {g.largest_driver||'movement'}</em></b>):<b>No material move yet</b>}</div><div><span>Most uncertain</span>{uncertain.map((g,i)=><b key={g.game_id||i}>{g.matchup||`${g.away_team} @ ${g.home_team}`} <em>{pct(g.pick_prob??g.pickP)}</em></b>)}</div></div></section>}

function GameCard({game,evidence=[],movement,confidence,onOpen}){return <article className="game-card" onClick={onOpen}><div className="game-top"><div><div className="kickoff">{kickoffText(game)}</div><div className="matchup"><span>{game.away_team}</span><em>@</em><span>{game.home_team}</span></div></div><div className="card-confidence"><ConfidenceRing row={confidence}/></div></div><ProbabilityBar game={game}/><div className="pick-line"><span>MODEL PICK</span><strong>{game.pick} {pct(game.pickP)}</strong>{movement&&Math.abs(n(movement.pick_delta_pp)||0)>.05&&<small className={n(movement.pick_delta_pp)>0?'up':'down'}>{n(movement.pick_delta_pp)>0?'↑':'↓'} {Math.abs(n(movement.pick_delta_pp)).toFixed(1)} pp</small>}</div><div className="game-metrics"><div><span>Projected</span><b>{game.projected_score||'—'}</b></div><div><span>Model line</span><b>{line(game.margin,game.home_team,game.away_team)}</b></div><div><span>Market</span><b>{line(game.market,game.home_team,game.away_team)}</b></div><div><span>Edge</span><b className={(game.pickEdge||0)>0?'positive':''}>{signed(game.pickEdge)}</b></div></div><div className="card-footer"><span>{evidence.length?`${evidence.length} verified context signals`:'Quantitative preview'}</span><button>Open game →</button></div></article>}

function EvidenceSection({title,items,empty}){return <div className="article-section"><span className="article-label">{title}</span>{items.length?items.map((e,i)=><div className="evidence" key={`${e.title}-${i}`}><span className={`strength ${strengthClass(e)}`}>{e.strength||'Context'}</span><b>{e.title}</b><p>{e.summary}</p><div className="evidence-meta">{e.sample_size&&<small>Sample: {e.sample_size}</small>}{e.relevance&&<small>{e.relevance}</small>}{e.as_of&&<small>As of {new Date(e.as_of).toLocaleString()}</small>}</div>{e.source_url&&<a href={e.source_url} target="_blank" rel="noreferrer">{e.source_name||'Source'} ↗</a>}</div>):<p>{empty}</p>}</div>}
function MovementPanel({m}){if(!m)return <div className="article-section"><span className="article-label">WHY THE FORECAST MOVED</span><p>No prior comparable run exists yet, so there is no movement to attribute.</p></div>;return <div className="article-section"><span className="article-label">WHY THE FORECAST MOVED</span><p>The pick moved <b>{pp(m.pick_delta_pp)}</b> since the previous valid run. The largest measured driver is <b>{m.largest_driver}</b>.</p><div className="driver-grid"><div><span>Model refresh</span><b>{pp(m.model_component_pp)}</b></div><div><span>Market</span><b>{pp(m.market_component_pp)}</b></div><div><span>Residual / transition</span><b>{pp(m.residual_component_pp)}</b></div><div><span>Margin</span><b>{signed(m.margin_delta)}</b></div></div><small className="guardrail-note">Attribution explains numeric run-to-run movement. Contextual news is only named as causal when the system can verify and link the source.</small></div>}
function ConfidencePanel({c}){if(!c)return null;return <div className="article-section"><span className="article-label">CONFIDENCE DECOMPOSITION</span><div className="confidence-breakdown"><ConfidenceRing row={c}/><div><p><b>Conviction:</b> {pct(c.confidence_conviction,0)} · distance from a 50/50 game.</p><p><b>Model agreement:</b> {pct(c.confidence_agreement,0)} · penalty for component-model dispersion.</p><p><b>Internal consistency:</b> {pct(c.confidence_consistency,0)} · whether win and margin heads tell the same story.</p></div></div><small className="guardrail-note">This 0–100 value is an explanatory confidence index, not another win probability. It will only be mapped to observed hit rates after sufficient locked 2026 history accumulates.</small></div>}

function GameModal({game,runs,evidence=[],movement,confidence,sources,onClose}){const history=evidence.filter(e=>['history','coaching','coordinator'].includes((e.category||'').toLowerCase()));const personnel=evidence.filter(e=>['injury','personnel'].includes((e.category||'').toLowerCase()));const scheme=evidence.filter(e=>['scheme','matchup'].includes((e.category||'').toLowerCase()));const scenario=evidence.filter(e=>['scenario','weather','travel'].includes((e.category||'').toLowerCase()));const trend=runs.filter(r=>r.game_id===game.game_id).map((r,i)=>({run:i+1,p:n(r.final_home_prob)})).filter(x=>x.p!=null);const opp=game.pick===game.home_team?game.away_team:game.home_team;return <div className="modal-backdrop" onClick={onClose}><div className="modal" onClick={e=>e.stopPropagation()}><button className="close" onClick={onClose}>×</button><div className="modal-kicker">{kickoffText(game)} • {game.confidence||'Forecast'} confidence</div><h2>{teamName(game.away_team)} <span>@</span> {teamName(game.home_team)}</h2><div className="score-pred"><span>MODEL PREDICTION</span><strong>{game.projected_score||`${game.pick} to win`}</strong><b>{game.pick} {pct(game.pickP)}</b></div><ProbabilityBar game={game}/><div className="article-grid"><article className="preview-copy"><div className="article-section"><span className="article-label">GAME PREVIEW</span><p>{teamName(game.pick)} is the current selection at <b>{pct(game.pickP)}</b>, with a central margin of <b>{line(game.margin,game.home_team,game.away_team)}</b> and expected total of <b>{one(game.expected_total)}</b>.</p><p>{game.market==null?'No current market spread is attached.':`The market is around ${line(game.market,game.home_team,game.away_team)}, leaving ${signed(game.pickEdge)} points of spread difference on the model's chosen side.`}</p></div><EvidenceSection title="PERSONNEL & AVAILABILITY" items={personnel} empty="No verified personnel signal is attached yet. The page will not infer injury impact from rumor or stale status."/><EvidenceSection title="SCHEME MATCHUP" items={scheme} empty="No verified tactical interaction has cleared the evidence filter yet."/><EvidenceSection title="HISTORY VS. WHAT'S DIFFERENT NOW" items={history} empty="No verified QB/coordinator or structural-history claim is attached yet. Small head-to-head samples are not manufactured into trends."/><EvidenceSection title="SCENARIO SENSITIVITY" items={scenario} empty="No material weather, travel, rest, or personnel scenario is active in the verified context feed."/><MovementPanel m={movement}/><ConfidencePanel c={confidence}/><div className="article-section"><span className="article-label">WHAT COULD MAKE US WRONG?</span><p>{teamName(opp)} can break the forecast by winning the highest-leverage downs, creating short fields, or forcing a game script outside the efficiency assumptions that drive the central projection. A win-margin split or elevated model disagreement should be treated as an additional warning.</p></div><div className="article-section"><span className="article-label">PREDICTION</span><p>{game.pick} to win, with {game.projected_score||'the displayed score distribution'} as the central forecast. The official version freezes inside the pregame lock window and is the only forecast graded later.</p></div></article><aside className="numbers-panel"><h3>The numbers</h3><Metric label="Sujar baseline" value={pct(game.sujar_home_prob)} sub={`${game.home_team} home win`}/><Metric label="PURE ensemble" value={pct(game.pure_home_prob)}/><Metric label="Market" value={pct(game.market_home_prob)}/><Metric label="Final MARKET+" value={pct(game.homeP)}/><Metric label="Fair home moneyline" value={game.fair_home_moneyline||american(game.homeP)}/><Metric label="Expected margin" value={line(game.margin,game.home_team,game.away_team)}/><Metric label="Expected total" value={one(game.expected_total)}/><Metric label="Home cover probability" value={pct(game.cover_home_prob)}/><Metric label="Over probability" value={pct(game.over_prob)}/><Metric label="Model disagreement" value={pct(game.model_disagreement)}/><Metric label="80% margin interval" value={n(game.margin_low_80)!=null?`${signed(game.margin_low_80)} to ${signed(game.margin_high_80)}`:'—'}/><SourceFreshness sources={sources}/></aside></div><section className="trend-section"><div className="section-head"><div><span>FORECAST HISTORY</span><h3>How the prediction moved</h3></div></div><div className="chart-box">{trend.length>1?<ResponsiveContainer width="100%" height={220}><AreaChart data={trend}><XAxis dataKey="run"/><YAxis domain={[0,1]} tickFormatter={v=>`${Math.round(v*100)}%`}/><Tooltip formatter={v=>pct(v)}/><Area dataKey="p" type="monotone" stroke="#55a8ff" fill="#55a8ff22" strokeWidth={3}/></AreaChart></ResponsiveContainer>:<div className="empty-chart">Movement appears after the second model run.</div>}</div></section></div></div>}
function Metric({label,value,sub}){return <div className="metric"><span>{label}</span><b>{value}</b>{sub&&<small>{sub}</small>}</div>}

function WeekPage({games,runs,evidence,brief,movement,confidence,status,sources,setSelected}){const moveMap=Object.fromEntries(movement.map(x=>[x.game_id,x]));const confMap=Object.fromEntries(confidence.map(x=>[x.game_id,x]));return <main><Hero games={games} status={status} brief={brief}/><WeeklyBrief brief={brief} games={games} movement={movement}/><section className="slate"><div className="section-head"><div><span>CURRENT SLATE</span><h2>Week {games[0]?.week||'—'} forecasts</h2></div><p>Every matchup includes the numbers, evidence, uncertainty, and movement attribution.</p></div><div className="game-grid">{games.map(g=><GameCard key={g.game_id} game={g} evidence={evidence[g.game_id]||[]} movement={moveMap[g.game_id]} confidence={confMap[g.game_id]} onOpen={()=>setSelected(g)}/>)}</div></section></main>}

function TeamsPage({profiles,setTeam}){const rows=[...profiles].sort((a,b)=>(n(a.rank)||999)-(n(b.rank)||999));return <main className="inner"><div className="page-head"><span>32 TEAM PROFILES</span><h1>Team intelligence</h1><p>One place for power rank, current efficiency state, trajectory, and the next model forecast.</p></div><div className="team-grid">{rows.map(r=><button className="team-card" key={r.team} onClick={()=>setTeam(r)}><span className="team-rank">#{r.rank}</span><h3>{r.team}</h3><p>{teamName(r.team)}</p><div><span>Elo+</span><b>{n(r.elo_plus)==null?'—':Math.round(n(r.elo_plus))}</b></div><div><span>Next</span><b>{r.next_opponent?`${r.next_site==='HOME'?'vs':'@'} ${r.next_opponent}`:'TBD'}</b></div><div><span>Win</span><b>{pct(r.next_win_prob)}</b></div></button>)}</div></main>}
function TeamModal({team,onClose}){return <div className="modal-backdrop" onClick={onClose}><div className="modal team-modal" onClick={e=>e.stopPropagation()}><button className="close" onClick={onClose}>×</button><div className="modal-kicker">TEAM PROFILE • POWER RANK #{team.rank}</div><h2>{teamName(team.team)}</h2><div className="team-profile-grid"><Metric label="Elo+" value={n(team.elo_plus)==null?'—':Math.round(n(team.elo_plus))}/><Metric label="Off EPA" value={n(team.off_epa)==null?'—':n(team.off_epa).toFixed(3)}/><Metric label="Def EPA allowed" value={n(team.def_epa_allowed)==null?'—':n(team.def_epa_allowed).toFixed(3)}/><Metric label="Pass EPA" value={n(team.pass_epa)==null?'—':n(team.pass_epa).toFixed(3)}/><Metric label="Recent win form" value={pct(team.recent_win_pct,0)}/><Metric label="Next win probability" value={pct(team.next_win_prob)}/><Metric label="Next expected margin" value={signed(team.next_expected_margin)}/><Metric label="Projected points" value={one(team.next_projected_points)}/></div><div className="article-section"><span className="article-label">NEXT MATCHUP</span><p>{team.next_opponent?`${team.team} is currently projected ${team.next_expected_margin>=0?'ahead of':'behind'} ${team.next_opponent} by ${Math.abs(n(team.next_expected_margin)||0).toFixed(1)} points, with a ${pct(team.next_win_prob)} win probability.`:'No upcoming game is currently published.'}</p></div></div></div>}

function RatingsPage({ratings}){const rows=[...ratings].sort((a,b)=>(n(a.rank)||999)-(n(b.rank)||999));return <main className="inner"><div className="page-head"><span>TEAM STRENGTH</span><h1>Power Ratings</h1><p>Elo+ remains the ranking spine until another composite demonstrably improves chronological out-of-sample prediction.</p></div><div className="table-card"><table><thead><tr><th>Rank</th><th>Team</th><th>Elo+</th><th>Move</th><th>Off EPA</th><th>Def EPA Allowed</th><th>Pass EPA</th><th>Recent W%</th></tr></thead><tbody>{rows.map(r=><tr key={r.team}><td>{r.rank}</td><td><b>{r.team}</b><small>{teamName(r.team)}</small></td><td><strong>{n(r.elo_plus)==null?'—':Math.round(n(r.elo_plus))}</strong></td><td>{r.movement||'—'}</td><td>{n(r.off_epa)==null?'—':n(r.off_epa).toFixed(3)}</td><td>{n(r.def_epa_allowed)==null?'—':n(r.def_epa_allowed).toFixed(3)}</td><td>{n(r.pass_epa)==null?'—':n(r.pass_epa).toFixed(3)}</td><td>{pct(r.recent_win_pct,0)}</td></tr>)}</tbody></table></div></main>}

function ModelsPage({leaderboard,games,calibration}){const models=[['sujar_home_prob','Sujar'],['logistic_home_prob','Logistic'],['extra_trees_home_prob','Extra Trees'],['xgboost_home_prob','XGBoost'],['catboost_home_prob','CatBoost'],['pure_home_prob','Core Stack'],['market_home_prob','Market'],['final_home_prob','Final']];const ece={};calibration.forEach(r=>{if(ece[r.model]==null&&n(r.ece)!=null)ece[r.model]=n(r.ece)});return <main className="inner"><div className="page-head"><span>MODEL LAB</span><h1>Consensus, calibration & validation</h1><p>Performance is chronological OOS. Calibration answers the harder question: when the model says 70%, does it actually behave like 70%?</p></div><section className="lab-section"><h2>Current-week consensus</h2><div className="table-card wide"><table><thead><tr><th>Game</th>{models.map(([,l])=><th key={l}>{l}</th>)}</tr></thead><tbody>{games.map(g=><tr key={g.game_id}><td><b>{g.away_team} @ {g.home_team}</b></td>{models.map(([c,l])=><td key={l}>{pct(g[c])}</td>)}</tr>)}</tbody></table></div></section><section className="lab-section"><h2>Walk-forward leaderboard</h2><div className="table-card"><table><thead><tr><th>Model</th><th>Games</th><th>Winner %</th><th>Brier ↓</th><th>Log Loss ↓</th><th>ECE ↓</th><th>Margin MAE ↓</th></tr></thead><tbody>{leaderboard.map(r=><tr key={r.model}><td><b>{r.model}</b><small>{r.status}</small></td><td>{r.games}</td><td>{pct(r.winner_pct)}</td><td>{n(r.brier)==null?'—':n(r.brier).toFixed(3)}</td><td>{n(r.log_loss)==null?'—':n(r.log_loss).toFixed(3)}</td><td>{ece[r.model]==null?'—':ece[r.model].toFixed(3)}</td><td>{one(r.margin_mae)}</td></tr>)}</tbody></table></div></section><section className="lab-section"><h2>Reliability buckets</h2><div className="calibration-grid">{Object.entries(Object.groupBy?Object.groupBy(calibration,r=>r.model):calibration.reduce((a,r)=>((a[r.model]??=[]).push(r),a),{})).map(([model,rows])=><div className="cal-card" key={model}><h3>{model}</h3><span>ECE {ece[model]==null?'—':ece[model].toFixed(3)}</span>{rows.map((r,i)=><div key={i}><b>{Math.round(n(r.avg_pred)*100)}%</b><div className="cal-track"><i style={{width:`${Math.max(0,Math.min(100,n(r.observed_home_win)*100))}%`}}/></div><em>{Math.round(n(r.observed_home_win)*100)}% observed · n={r.games}</em></div>)}</div>)}</div></section></main>}

function HistoryPage({history,autopsies}){return <main className="inner"><div className="page-head"><span>ACCOUNTABILITY</span><h1>Official prediction history</h1><p>Only immutable pregame locks are graded. Every completed game gets a quantitative autopsy; causal football explanations wait for verified current-season play-level evidence.</p></div><div className="history-grid">{history.length?history.map(r=>{const a=autopsies[r.game_id];return <article className="history-card" key={r.game_id}><div><span>{r.away_team} @ {r.home_team}</span><b>{r.pick} · {pct(r.pick===r.home_team?n(r.final_home_prob):1-n(r.final_home_prob))}</b></div><p>Projected: {r.projected_score||'—'}</p><p>Actual: {a?.actual_score||'Pending'}</p>{a&&<><div className={a.winner_correct?'result-good':'result-bad'}>{a.winner_correct?'✓ Winner correct':'✕ Winner missed'}</div><small>Margin error {one(a.margin_abs_error)} · Total error {one(a.total_abs_error)}</small><details><summary>Postgame autopsy</summary>{a.what_went_right?.map((x,i)=><p key={`r${i}`}>✓ {x}</p>)}{a.what_missed?.map((x,i)=><p key={`m${i}`}>• {x}</p>)}<em>{a.causal_analysis_status}</em></details></>}</article>}):<div className="empty-state"><b>Official history begins at lock.</b></div>}</div></main>}

function MethodPage(){return <main className="inner methodology"><div className="page-head"><span>HOW IT WORKS</span><h1>Prediction first. Narrative second.</h1><p>The system is designed to be football-smart without letting a compelling story contaminate the forecast.</p></div><div className="method-grid"><Method n="01" title="Quantitative forecast" text="Chronological models estimate winner, margin and total independently."/><Method n="02" title="Context evidence" text="Personnel, scheme, coaching history, travel and weather are sourced and strength-labeled."/><Method n="03" title="Movement attribution" text="Run-to-run changes are decomposed into model refresh, market movement and residual transitions."/><Method n="04" title="Calibration" text="Reliability tables test whether stated probabilities match observed frequencies out of sample."/><Method n="05" title="Promotion rule" text="Context only enters the numerical model after a feature improves chronological OOS performance."/><Method n="06" title="Immutable accountability" text="The pregame lock is preserved permanently and is the only version graded."/></div><section className="guardrails"><h2>Guardrails</h2><ul><li>No fabricated injury, coordinator or scheme claims.</li><li>Source links and timestamps for contextual facts.</li><li>Historical samples are labeled by relevance and size.</li><li>Confidence index is not mislabeled as probability.</li><li>No player-prop product creep.</li></ul></section></main>}
function Method({n,title,text}){return <div className="method-card"><span>{n}</span><h3>{title}</h3><p>{text}</p></div>}

export default function AppV2(){const[tab,setTab]=useState('week'),[selected,setSelected]=useState(null),[team,setTeam]=useState(null),[loading,setLoading]=useState(true),[data,setData]=useState({games:[],runs:[],ratings:[],leaderboard:[],history:[],evidence:{},status:{},sources:{},calibration:[],confidence:[],movement:[],profiles:[],brief:{},autopsies:{}});useEffect(()=>{let active=true;Promise.all([fetchCSV('this_week.csv'),fetchCSV('run_history.csv'),fetchCSV('power_ratings.csv'),fetchCSV('model_leaderboard.csv'),fetchCSV('prediction_history.csv'),fetchJSON('contextual_evidence.json',{}),fetchJSON('status.json',{}),fetchJSON('context_source_status.json',{}),fetchCSV('calibration.csv'),fetchCSV('confidence_diagnostics.csv'),fetchCSV('movement_attribution.csv'),fetchCSV('team_profiles.csv'),fetchJSON('weekly_brief.json',{}),fetchJSON('postgame_autopsies.json',{})]).then(([games,runs,ratings,leaderboard,history,evidence,status,sources,calibration,confidence,movement,profiles,brief,autopsies])=>{if(!active)return;setData({games:games.map(normalizeGame),runs,ratings,leaderboard,history,evidence,status,sources,calibration,confidence,movement,profiles,brief,autopsies});setLoading(false)});return()=>{active=false}},[]);const page=useMemo(()=>{if(tab==='teams')return <TeamsPage profiles={data.profiles.length?data.profiles:data.ratings} setTeam={setTeam}/>;if(tab==='ratings')return <RatingsPage ratings={data.ratings}/>;if(tab==='models')return <ModelsPage leaderboard={data.leaderboard} games={data.games} calibration={data.calibration}/>;if(tab==='history')return <HistoryPage history={data.history} autopsies={data.autopsies}/>;if(tab==='method')return <MethodPage/>;return <WeekPage games={data.games} runs={data.runs} evidence={data.evidence} brief={data.brief} movement={data.movement} confidence={data.confidence} status={data.status} sources={data.sources} setSelected={setSelected}/>},[tab,data]);if(loading)return <div className="loading"><div className="loader"/><b>Loading the latest forecast</b></div>;const moveMap=Object.fromEntries(data.movement.map(x=>[x.game_id,x])),confMap=Object.fromEntries(data.confidence.map(x=>[x.game_id,x]));return <div className="app"><Header tab={tab} setTab={setTab} status={data.status}/>{page}{selected&&<GameModal game={selected} runs={data.runs} evidence={data.evidence[selected.game_id]||[]} movement={moveMap[selected.game_id]} confidence={confMap[selected.game_id]} sources={data.sources} onClose={()=>setSelected(null)}/>} {team&&<TeamModal team={team} onClose={()=>setTeam(null)}/>}<footer><b>NFL FORECAST</b><span>Free-data quantitative forecasting • Live forecasts are not official until locked.</span></footer></div>}
