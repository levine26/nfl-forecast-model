# LevLine Props integration validation

This branch exists only to run the integrated research-beta validation suite against the current research/props-integration head. It must never target or merge into main.
