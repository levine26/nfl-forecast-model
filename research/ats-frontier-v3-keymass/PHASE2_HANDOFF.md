# Phase 2 Handoff

Phase 2 starts only after the final Phase-1 receipt is merged.

Exact first action: execute the frozen 2010–2025 parameter fit, record the generated parameter-file hash without inspecting any 2026 outcome, then consume qualifying T-120 market snapshots to append one immutable candidate/null shadow prediction per eligible future game. Do not score outcomes.

Based on the current Week-3 schedule surface available at Phase-1 freeze, the first potential eligible window is `2026_03_ATL_GB` on September 24, 2026, whose T-120 target occurs after the scientific freeze. It is eligible only if the governed T-120 timestamp/book-count contract is actually satisfied; otherwise the next qualifying future game becomes first.