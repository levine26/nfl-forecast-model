# Phase 2 Handoff — Controlled Implementation & Historical Development

**Phase 2 status:** NOT STARTED  
**This file is an instruction boundary, not authorization to execute Phase 2 in Phase 1.**

## 1. Read-first order

A future Phase-2 chat must read, in order:

1. `MASTER_PLAN.md`;
2. `FINAL_PHASE1_RECEIPT.md`;
3. `PHASE_STATUS.md`;
4. `CHRONOLOGY_AND_EVIDENCE_BOUNDARY.md`;
5. `DATA_AND_PIT_INVENTORY.md`;
6. Q1/Q2/Q3 preregistrations;
7. `EVALUATION_PROTOCOL.md`;
8. `ATS_ECONOMICS_AND_EV_CONTRACT.md`;
9. `RED_TEAM_AND_LEAKAGE_CHECKLIST.md`.

If any proposed implementation conflicts with those files, amend/version the scientific contract **before** producing candidate performance.

## 2. Exact first action

Create a dedicated Phase-2 research branch from then-current `main`.

Before fitting Q1:

- implement canonical sign/ATS grading helpers under research-only surfaces;
- add the four synthetic sign tests in Q1 preregistration;
- assert whole/half-line push logic;
- build a row-provenance manifest for spread/total/moneyline and football features;
- assert outer/inner chronology;
- assert no completed 2026 outcomes enter the historical candidate frame;
- freeze code/config digests and write a pre-result Phase-2 opening receipt.

Only after those gates pass may Q1 historical development run.

## 3. Required implementation sequence

### Stage A — Q1 only

Implement `ATS-Q1-QUANTILE-MARKET-RESIDUAL-V1` exactly. Generate chronology-clean 2022–2025 outer OOF results only after the Q1 contract tests pass.

Do not redesign Q1 after seeing those results.

### Stage B — Q2

Implement Q2 from the frozen Q1 OOF interface. Q2 must never consume an in-sample Q1 center for an outer target row. Add PMF mass/sum/monotonicity/push tests before Q2 result execution.

### Stage C — Q3

Implement the two-part hurdle exactly. Prove half-point structural zero push and three-outcome normalization before execution.

### Stage D — blend/evaluation

Construct only the fixed `{0,.25,.50,.75,1}` Q2 weight grid from prior inner OOF multinomial log loss. Run exact common-row M0/M1/M2/Q1/Q2/Q3/blend reporting and fixed selective/key-number slices.

## 4. Forbidden Phase-2 rescue

Do not add:

- boosted-tree/GAM/neural learner;
- new quantile;
- new key number;
- mixture distribution;
- new feature family;
- personnel reconstruction;
- post-hoc calibration;
- new edge threshold;
- new selective percentile;
- completed-2026 outcome signal;
- historical multi-book/juice values that cannot be proven.

A scientifically necessary pre-result engineering correction must be recorded with reason and candidate-version impact. A correction motivated by poor target results is a new experiment, not a continuation.

## 5. Deliverables

Phase 2 must preserve:

- pre-result opening receipt;
- implementation/config hashes;
- exact feature/provenance manifest;
- chronology manifests;
- Q1/Q2/Q3 OOF predictions;
- market-null OOF predictions;
- inner tuning selections;
- distribution/key-number fits by outer season;
- proper-score/calibration evidence;
- selective ATS evidence;
- block-bootstrap uncertainty;
- complete negative results;
- red-team checklist results;
- Phase-3 handoff.

## 6. Production boundary

Phase 2 is research only. It may not modify F-ST, Sunday Signal production numerical forecasting, official public fair-spread behavior, locks/history or grading.